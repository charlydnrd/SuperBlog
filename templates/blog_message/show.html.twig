{% extends 'base.html.twig' %}

{% block title %}BlogMessage{% endblock %}

{% block body %}
    <h1>BlogMessage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ blog_message.id }}</td>
            </tr>
            <tr>
                <th>Blog</th>
                <td>{{ blog_message.blog }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('blog_message_index') }}">back to list</a>

    <a href="{{ path('blog_message_edit', {'id': blog_message.id}) }}">edit</a>

    {{ include('blog_message/_delete_form.html.twig') }}
{% endblock %}
